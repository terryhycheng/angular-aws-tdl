{
  "name": "lambda-testing-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "npx eslint .",
    "deploy": "npm install -f && npx prisma generate && serverless deploy"
  },
  "dependencies": {
    "@prisma/client": "^4.1.1",
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "serverless-http": "^2.7.0"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^4.0.1"
  }
}
